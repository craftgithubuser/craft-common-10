create schema if not exists studentinfo;
use studentinfo;
create schema studentinfo; use studentinfo; 
create table student( studentid int, studentname varchar(20),
 parentname varchar(20), adress varchar (40), postalcode int, city varchar (10) );
